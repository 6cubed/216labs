# 216labs — Caddy reverse proxy
#
# Set DOMAIN in your .env (e.g. DOMAIN=216labs.com).
# Caddy auto-provisions Let's Encrypt HTTPS for each subdomain.
# On localhost, set DOMAIN=localhost to use HTTP without certs.

ramblingradio.{$DOMAIN} {
	reverse_proxy ramblingradio:5000
}

stroll.{$DOMAIN} {
	reverse_proxy stroll:5000
}

onefit.{$DOMAIN} {
	reverse_proxy onefit:3000
}

paperframe.{$DOMAIN} {
	reverse_proxy paperframe-frontend:3000
}
