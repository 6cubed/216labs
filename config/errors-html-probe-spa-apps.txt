# Vite/SPA apps: reporter lives in the main JS bundle, not inline in index.html.
# heartbeat-stack-check.sh fetches the first /assets/*.js (or *.js script) and greps for report-error.
ytsync
onefit
tortellini
