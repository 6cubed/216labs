# App probes via SSH + docker exec (localhost inside container). Format: app_id|port
# Use when public URL redirects through activator or HTML is injected at runtime only.
birdperch|8000
pipesecure|3000
