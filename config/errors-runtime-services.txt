# Compose service names whose images must ship node_modules/@216labs/errors/dist/*.cjs.
# Used by GHCR CI (post-build verify) and the activator (skip retag of broken GHCR :latest).
# Override at runtime: ACTIVATOR_ERRORS_RUNTIME_SERVICES=ramblingradio,stroll
ramblingradio
stroll
ytsync
