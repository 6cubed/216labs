export default function Page() {
  return (
    <main>
      <h1 style={{ fontSize: "1.5rem", marginBottom: "0.75rem" }}>Hello from Next.js</h1>
      <p style={{ color: "#444", maxWidth: "36rem" }}>
        This is a minimal demo app. It uses the same Docker standalone build, manifest, Caddy routing, and admin
        registration as every other service in the repo. Replace it with your own product or delete the folder when
        you no longer need the reference.
      </p>
    </main>
  );
}
